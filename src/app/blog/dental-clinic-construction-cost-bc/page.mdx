import { blogMetadata } from "@/lib/blog";

export const metadata = blogMetadata("dental-clinic-construction-cost-bc");

# Pilot

Checking that MDX composes with `output: "export"`.

| Line item | Cost |
| --- | --- |
| Test | $1 |
